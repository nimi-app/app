query getDefaultLensProfile($account: EthereumAddress!, $chainId: Int) {
  defaultProfile(request: { ethereumAddress: $account }) {
    name
    bio
    picture {
      ... on NftImage {
        uri
      }
      ... on MediaSet {
        original {
          url
        }
      }
    }
  }
}
